import numpy as np

def conduction_velocity(diameter):
    # Simplified model for conduction velocity
    return np.sqrt(diameter)

diameters = np.linspace(1, 10, 100)
velocities = [conduction_velocity(d) for d in diameters]